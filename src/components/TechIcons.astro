---
const { XAxis, YAxis, color } = Astro.props
---
<div class=`absolute` style=`transform: translate(${ XAxis }px, ${ YAxis }px);`>
    <div class="relative flex items-center justify-center">
        <div class="absolute z-0 w-9 h-9 blur-md" style=`background-color: #${ color };`>
        </div>
        <slot />
    </div>
</div>